<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Financial Literacy Dashboard</title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <%- include('./partials/sidebar', { activePage: 'dashboard' }) %>
    <%- include('./partials/header', { user: user || null, notifications: notifications || [], unreadCount: unreadCount || 0 }) %>

    <main class="pt-16 md:ml-64 min-h-screen">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <!-- Welcome Section -->
            <div class="bg-gradient-to-r from-blue-600 to-blue-500 rounded-lg p-6 text-white mb-6">
                <h1 class="text-2xl font-bold">Welcome, <%= user.username %>!</h1>
                <p class="mt-1 opacity-90">Continue your journey to financial freedom.</p>
            </div>

            <!-- Learning Progress -->
            <div class="mb-8">
                <div class="bg-white rounded-lg border border-gray-200 p-6 flex flex-col md:flex-row items-center gap-6">
                    <div class="flex-1">
                        <h3 class="text-lg font-semibold text-gray-900 mb-2">Learning Progress</h3>
                        <% if (user.enrolledCourses.length > 0) { %>
                        <div class="w-full bg-gray-100 rounded-full h-4 mb-2">
                            <div class="bg-blue-600 h-4 rounded-full" style="width: <%= Math.round(user.enrolledCourses.reduce((sum, course) => sum + course.progress, 0) / user.enrolledCourses.length) %>%;"></div>
                        </div>
                        <span class="text-blue-700 font-bold"><%= Math.round(user.enrolledCourses.reduce((sum, course) => sum + course.progress, 0) / user.enrolledCourses.length) %></span>%
                        <% } else { %>
                        <div class="w-full bg-gray-100 rounded-full h-4 mb-2">
                            <div class="bg-blue-200 h-4 rounded-full" style="width: 0%"></div>
                        </div>
                        <span class="text-gray-500">0%</span>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- Enrolled Courses Section -->
            <div class="mb-8">
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xl font-bold text-gray-900">Your Enrolled Courses</h2>
                    <a href="/courses" class="text-blue-600 hover:underline">View All</a>
                </div>
                <% if (user.enrolledCourses.length > 0) { %>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <% user.enrolledCourses.slice(0, 2).forEach(course => { %>
                    <div class="bg-white rounded-lg border border-gray-200 p-6 flex flex-col">
                        <h3 class="text-lg font-semibold text-gray-900 mb-2"><%= course.title %></h3>
                        <p class="text-gray-600 mb-4"><%= course.description || 'Learn more about this course' %></p>
                        <div class="flex items-center justify-between mt-auto">
                            <span class="text-sm text-blue-700 font-bold"><%= course.progress %>% Complete</span>
                            <a href="/courses/<%= course._id %>" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm rounded-md hover:bg-blue-700 transition-colors">Continue</a>
                        </div>
                    </div>
                    <% }); %>
                </div>
                <% } else { %>
                <div class="bg-white rounded-lg border border-gray-200 p-8 text-center">
                    <p class="text-gray-600">No courses enrolled yet. Browse our available courses below.</p>
                </div>
                <% } %>
            </div>

            <!-- Available Courses Section -->
            <div class="mb-8">
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xl font-bold text-gray-900">Available Courses</h2>
                    <a href="/courses" class="text-blue-600 hover:underline">View All Available Courses</a>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <% availableCourses.slice(0, 2).forEach(course => { %>
                    <div class="bg-white rounded-lg border border-gray-200 p-6 flex flex-col">
                        <h3 class="text-lg font-semibold text-gray-900 mb-2"><%= course.title %></h3>
                        <p class="text-gray-600 mb-4"><%= course.description %></p>
                        <div class="flex items-center justify-between mt-auto">
                            <span class="text-sm text-gray-500"><%= course.enrolledCount || 0 %> enrolled</span>
                            <a href="/courses/<%= course._id %>" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm rounded-md hover:bg-blue-700 transition-colors">View Course</a>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>

            <!-- Blog Section -->
            <div>
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xl font-bold text-gray-900">Latest From Our Blog</h2>
                    <a href="/blog" class="text-blue-600 hover:underline">View All Posts</a>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="bg-white rounded-lg border border-gray-200 p-6 flex flex-col">
                        <h3 class="text-lg font-semibold text-gray-900 mb-2">Understanding Market Trends in 2025</h3>
                        <p class="text-gray-600 mb-4">An analysis of current market conditions and forecasts for the remainder of the year.</p>
                        <a href="/blog/market-trends" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm rounded-md hover:bg-blue-700 transition-colors">Read More</a>
                    </div>
                    <div class="bg-white rounded-lg border border-gray-200 p-6 flex flex-col">
                        <h3 class="text-lg font-semibold text-gray-900 mb-2">The Future of Cryptocurrency Investing</h3>
                        <p class="text-gray-600 mb-4">Exploring the potential and risks of digital assets in a balanced portfolio.</p>
                        <a href="/blog/crypto-investing" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm rounded-md hover:bg-blue-700 transition-colors">Read More</a>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>
</html>