<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Blog Post | FinLit</title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Include Quill.js for rich text editing -->
  <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
  <!-- Include Expert Sidebar -->
  <%- include('../../partials/expert-sidebar', { activePage: 'blog' }) %>
  <!-- Include Header -->
  <%- include('../../partials/expert-header') %>

  <!-- Main Content -->
  <main class="pt-16 md:ml-64 min-h-screen transition-all duration-300 md:ml-64">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <!-- Page Header -->
      <div class="flex items-center justify-between mb-6 text-white px-4 py-2 bg-gradient-to-r from-blue-600 to-blue-500 rounded-lg">
        <div>
          <h1 class="text-2xl font-bold">Create Blog Post</h1>
          <p class="mt-1 text-sm">Share your financial expertise with the community</p>
        </div>
        <a href="/expert/blog" class="inline-flex items-center justify-center px-4 py-2 text-sm font-medium text-blue-600 bg-white rounded-md hover:bg-blue-50 transition-colors">
          <i class="fas fa-arrow-left mr-1.5"></i> Back to Blog
        </a>
      </div>
      
      <!-- Create Post Form -->
      <div class="bg-white rounded-lg border border-gray-200 shadow-sm overflow-hidden">
        <form action="/blog/expert/create" method="POST" id="createPostForm">
          <!-- Form Header -->
          <div class="px-6 py-4 bg-gray-50 border-b border-gray-200">
            <h2 class="font-medium text-gray-900">Post Details</h2>
          </div>
          
          <div class="p-6 space-y-6">
            <!-- Title Input -->
            <div>
              <label for="title" class="block text-sm font-medium text-gray-700 mb-1">Title<span class="text-red-500">*</span>
              </label>
              <input 
                type="text" 
                id="title" 
                name="title" 
                required 
                placeholder="Enter a descriptive title"
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
              >
              <p class="mt-1 text-xs text-gray-500">Make your title specific and clear (150 characters max)</p>
            </div>
            
            <!-- Category Selection -->
            <div>
              <label 
                for="category" 
                class="block text-sm font-medium text-gray-700 mb-1">Category<span class="text-red-500">*</span></label>
              <select 
                id="category" 
                name="category" 
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
              >
                <option value="" disabled selected>Select a category</option>
                <option value="Savings">Savings</option>
                <option value="Investing">Investing</option>
                <option value="Budgeting">Budgeting</option>
                <option value="Debt Management">Debt Management</option>
                <option value="Retirement">Retirement</option>
                <option value="Financial Planning">Financial Planning</option>
                <option value="Personal Finance">Personal Finance</option>
                <option value="Credit">Credit</option>
                <option value="Taxes">Taxes</option>
                <option value="Insurance">Insurance</option>
                <option value="Real Estate">Real Estate</option>
                <option value="Other">Other</option>
              </select>
            </div>
            
            <!-- Rich Text Editor -->
            <div class="space-y-2">
              <label for="editor" class="block text-sm font-medium text-gray-700 mb-1">Content <span class="text-red-500">*</span></label>
              <div id="editor" class="h-64 border border-gray-300 rounded-md"></div>
              <input type="hidden" name="content" id="content">
            </div>
          </div>
          
          <!-- Form Actions -->
          <div class="px-6 py-4 bg-gray-50 border-t border-gray-200">
            <div class="flex gap-4">
              <button 
                type="submit" 
                name="status"
                value="published"
                class="<%= !isNewDraft ? 'bg-blue-600 text-white hover:bg-blue-700' : 'bg-gray-200 text-gray-700 hover:bg-gray-300' %> px-4 py-2 text-sm rounded-md shadow-sm transition-colors"
              >
                <i class="fas fa-paper-plane mr-2"></i> Publish
              </button>
              <button 
                type="submit"
                name="status"
                value="draft" 
                class="<%= isNewDraft ? 'bg-blue-600 text-white hover:bg-blue-700' : 'bg-gray-200 text-gray-700 hover:bg-gray-300' %> px-4 py-2 text-sm rounded-md shadow-sm transition-colors"
              >
                <i class="fas fa-save mr-2"></i> Save as Draft
              </button>
              <button 
                type="button" 
                onclick="window.location.href='/blog/expert'" 
                class="px-4 py-2 text-sm text-gray-700 bg-white border border-gray-300 rounded-md shadow-sm hover:bg-gray-50"
              >
                <i class="fas fa-times mr-2"></i> Cancel
              </button>
            </div>
          </div>
        </form>
      </div>
      
      <!-- Tips Card -->
      <div class="mt-6 bg-blue-50 border rounded-md p-4">
        <h2 class="text-lg font-semibold text-gray-800 mb-2 flex items-center gap-4">Tips for Creating Engaging Posts
        </h2>
        <ul class="text-sm text-gray-600 space-y-4">
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Use a clear, specific title that summarizes your topic</span>
          </li>
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Break content into paragraphs for better readability</span>
          </li>
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Use formatting (bold, lists) to highlight important points</span>
          </li>
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Include relevant information and avoid unnecessary details</span>
          </li>
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Be respectful and follow community guidelines</span>
          </li>
        </ul>
      </div>
    </div>
  </main>

  <!-- Include Quill.js for rich text editing -->
  <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Toggle mobile sidebar
      const sidebarToggle = document.getElementById('sidebarToggle');
      const sidebar = document.querySelector('nav');
      
      if (sidebarToggle && sidebar) {
        sidebarToggle.addEventListener('click', function() {
          sidebar.classList.toggle('-translate-x-full');
        });
      }

      // Initialize Quill editor
      const quill = new Quill('#editor', {
        theme: 'snow',
        placeholder: 'Share your expertise and knowledge here...',
        modules: {
          toolbar: [
            ['bold', 'italic', 'underline'],
            [{ 'list': 'ordered'}, { 'list': 'bullet' }],
            ['link'],
            ['clean']
          ]
        }
      });

      // Handle form submission
      const form = document.getElementById('createPostForm');
      form.onsubmit = function() {
        const content = document.getElementById('content');
        content.value = quill.root.innerHTML;
        return true;
      };
    });
  </script>
  <%- include('../../partials/expert-footer') %>
</body>
</html>