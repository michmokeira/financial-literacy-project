<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Forum Post | FinLit</title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Include Quill.js for rich text editing -->
  <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
  <!-- Include Expert Sidebar -->
  <%- include('../../partials/expert-sidebar', { activePage: 'forum' }) %>
  <!-- Include Header -->
  <%- include('../../partials/expert-header') %>

  <!-- Main Content -->
  <main class="pt-16 md:ml-64 min-h-screen transition-all duration-300 md:ml-64">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <!-- Page Header -->
      <div class="flex items-center justify-between mb-6 text-white px-4 py-2 bg-gradient-to-r from-blue-600 to-blue-500 rounded-lg">
        <div>
          <h1 class="text-2xl font-bold">Create Forum Post</h1>
          <p class="mt-1 text-sm">Share your financial expertise with the community</p>
        </div>
        <a href="/expert/forum" class="inline-flex items-center justify-center px-4 py-2 text-sm font-medium text-blue-600 bg-white rounded-md hover:bg-blue-50 transition-colors">
          <i class="fas fa-arrow-left mr-1.5"></i> Back to Forum
        </a>
      </div>
      
      <!-- Create Post Form -->
      <div class="bg-white rounded-lg border border-gray-200 shadow-sm overflow-hidden">
        <form action="/expert/forum/create" method="POST" id="createPostForm">
          <!-- Form Header -->
          <div class="px-6 py-4 bg-gray-50 border-b border-gray-200">
            <h2 class="font-medium text-gray-900">Post Details</h2>
          </div>
          
          <div class="p-6 space-y-6">
            <!-- Title Input -->
            <div>
              <label for="title" class="block text-sm font-medium text-gray-700 mb-1">Title<span class="text-red-500">*</span>
              </label>
              <input 
                type="text" 
                id="title" 
                name="title" 
                required 
                placeholder="Enter a descriptive title"
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
              >
              <p class="mt-1 text-xs text-gray-500">Make your title specific and clear (150 characters max)</p>
            </div>
            
            <!-- Category Selection -->
            <div>
              <label 
                for="category" 
                class="block text-sm font-medium text-gray-700 mb-1">Category<span class="text-red-500">*</span></label>
              <select 
                id="category" 
                name="category" 
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
              >
                <option value="" disabled selected>Select a category</option>
                <option value="investing">Investing</option>
                <option value="saving">Saving</option>
                <option value="budgeting">Budgeting</option>
                <option value="debt">Debt Management</option>
                <option value="retirement">Retirement Planning</option>
                <option value="taxes">Taxes</option>
                <option value="insurance">Insurance</option>
                <option value="credit">Credit & Credit Cards</option>
                <option value="mortgage">Mortgage & Real Estate</option>
                <option value="cryptocurrency">Cryptocurrency</option>
                <option value="stocks">Stocks & Trading</option>
                <option value="other">Other</option>
              </select>
            </div>
            
            <!-- Tags Input -->
            <div>
              <label for="tags" class="block text-sm font-medium text-gray-700 mb-1">Tags</label>
              <input type="text" id="tags" name="tags" 
                class="w-full rounded-md border-gray-300 focus:border-blue-500 focus:ring focus:ring-blue-200 focus:ring-opacity-50" 
                placeholder="e.g. investment, retirement, savings">
              <p class="mt-1 text-xs text-gray-500">Separate tags with commas (maximum 5 tags)</p>
            </div>
            
            <!-- Rich Text Editor -->
            <div class="space-y-2">
              <label for="editor" class="block text-sm font-medium text-gray-700 mb-1">Content <span class="text-red-500">*</span></label>
              <div id="editor" class="h-64 border border-gray-300 rounded-md"></div>
              <input type="hidden" name="content" id="content">
            </div>
            
                      
          <!-- Form Actions -->
          <div class="flex gap-4 pt-2">
            <button 
              type="submit" 
              id="submitButton" 
              class="px-4 py-2 text-sm text-white bg-blue-600 rounded-md shadow-sm hover:bg-blue-700">
              <i class="fas fa-paper-plane mr-2"></i> Create Post
            </button>
            <button 
              type="button" 
              onclick="window.location.href='/expert/forum'" 
              class="px-4 py-2 text-sm text-gray-700 bg-white border border-gray-300 rounded-md shadow-sm hover:bg-gray-50 mr-2">
              <i class="fas fa-times mr-2"></i> Cancel
            </button>
            
          </div>
        </form>
      </div>
      
      <!-- Tips Card -->
      <div class="bg-blue-50 border border-gray-200 p-4">
        <h2 class="text-lg font-semibold text-gray-800 mb-2 flex items-center gap-4">Tips for Creating Engaging Posts
        </h2>
        <ul class="text-sm text-gray-600 space-y-4">
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Use a clear, specific title that summarizes your topic</span>
          </li>
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Break content into paragraphs for better readability</span>
          </li>
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Use formatting (bold, lists) to highlight important points</span>
          </li>
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Include relevant information and avoid unnecessary details</span>
          </li>
          <li class="flex items-start gap-4">
            <i class="fas fa-check-circle mt-1 mr-2"></i>
            <span>Be respectful and follow community guidelines</span>
          </li>
        </ul>
      </div>
    </div>
  </main>

  <!-- Include Quill.js for rich text editing -->
  <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Toggle mobile sidebar
      const sidebarToggle = document.getElementById('sidebarToggle');
      const sidebar = document.querySelector('nav');
      
      if (sidebarToggle && sidebar) {
        sidebarToggle.addEventListener('click', function() {
          sidebar.classList.toggle('-translate-x-full');
        });
      }

      // Initialize Quill editor
      const quill = new Quill('#editor', {
        theme: 'snow',
        placeholder: 'Share your expertise, advice, or question here...',
        modules: {
          toolbar: [
            ['bold', 'italic', 'underline'],
            [{ 'list': 'ordered'}, { 'list': 'bullet' }],
            ['link'],
            ['clean']
          ]
        }
      });

      // Update hidden input with editor content before form submission
      const form = document.getElementById('createPostForm');
      form.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // Get content from Quill editor
        const content = document.getElementById('content');
        content.value = quill.root.innerHTML;
        
        // Check if title and content are provided
        const title = document.getElementById('title').value.trim();
        const category = document.getElementById('category').value;
        
        if (!title) {
          alert('Please enter a title for your post.');
          return;
        }
        
        if (!category) {
          alert('Please select a category for your post.');
          return;
        }
        
        if (quill.getText().trim().length < 10) {
          alert('Please enter some content for your post.');
          return;
        }
        
        // Limit tags to 5
        const tagsInput = document.getElementById('tags').value;
        if (tagsInput) {
          const tagsArray = tagsInput.split(',').map(tag => tag.trim()).filter(tag => tag);
          if (tagsArray.length > 5) {
            alert('Please limit your post to 5 tags maximum.');
            return;
          }
        }
        
        // Update button text to show loading state
        const submitButton = document.getElementById('submitButton');
        submitButton.innerHTML = '<i class="fas fa-circle-notch fa-spin mr-1.5"></i> Publishing...';
        submitButton.disabled = true;
        
        // Submit the form
        this.submit();
      });
    });
  </script>
  <%- include('../../partials/expert-footer') %>
</body>
</html>